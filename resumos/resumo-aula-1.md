## Aula 01
```
git init
git config (--global/--system/--local) user.name
git config --global user.email
git config --global credential.helper (store/cache)
git clone --branch feature-1 --single-branch (clone apenas desta branch)
```



